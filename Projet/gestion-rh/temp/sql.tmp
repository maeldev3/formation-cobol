SELECT e.employee_id || ' | ' || e.first_name || ' ' || e.last_name || ' | ' || d.department_name || ' | ' || p.position_name FROM employees e, departments d, positions p WHERE e.department_id = d.department_id AND e.position_id = p.position_id;
